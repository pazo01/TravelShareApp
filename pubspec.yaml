name: travelshare
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.10.0
  flutter_riverpod: ^2.6.1
  google_sign_in: ^7.1.1

  # Giorno 4: AGGIUNTE (qui sotto, NON sotto 'flutter:')
  http: ^1.2.2
  flutter_dotenv: ^5.1.0
  geolocator: ^12.0.0
  geocoding: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  # (Non mettere pacchetti qui dentro; qui vanno solo cose tipo assets:, fonts:, etc.)
